name: flutter_design_lint
description: Flutter design quality lint rules — enforces design token usage, RTL support, and typography consistency. Zero false positives.
version: 1.0.0
homepage: https://github.com/abdurrahmanj/flutter-design-anti-patterns

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  analyzer: ^6.0.0
  custom_lint_builder: ^0.6.0

dev_dependencies:
  lints: ^3.0.0
